using UnityEngine;
using System.Collections;

public enum SceneType { Menu, Game, RankingAdd, RankingList, Settings };

public enum EnemyType { Big = 0, Normal = 1 };

public enum ShotType { Single = 0, Triple = 1 };

public enum PowerUpType { Speed = 0, Shield = 1, TripleShot = 2 };
